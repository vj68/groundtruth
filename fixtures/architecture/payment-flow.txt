Order API -> Payment Worker -> Payment Provider -> Acknowledgement -> Local Completion Store

Queue delivery is at-least-once. Provider capture and local completion persistence are not
atomic. Retried logical payment operations must therefore reuse a stable provider idempotency
key.

